# Text Encoders

Place downloaded text encoder model folders here.

This repository intentionally does not track text encoder weights or model files. The Docker services mount this directory at `/workspace/text-encoders`.
